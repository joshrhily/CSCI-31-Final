export default function PageTitle({ title }) {
  return (
    <div className="mx-12 pt-6">
      <h1 className="flex justify-center text-4xl font-serif font-bold mb-6 pl-8">{title}</h1>
    </div>
  )
}
